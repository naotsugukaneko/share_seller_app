<%= form_with model: @post, local: true do |f| %>
  <p><%= f.text_area :body, required: true %></p>
  <p><%= f.file_field :image, accept: "image/png,image/jpeg,image/gif", required: true %></p>
  <p><%= f.submit "送信" %></p>
<% end %>
